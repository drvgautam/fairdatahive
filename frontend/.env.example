# Leave empty to use Vite proxy (/api → http://localhost:8002)
VITE_API_BASE=

# Override API target if the backend is not on port 8002
# VITE_API_PROXY=http://localhost:8002

# Set to "true" to send Bearer dev (works with DEV_AUTH_ENABLED on API)
VITE_DEV_AUTH=true
